# Test fixtures

A throwaway RSA-2048 pair, generated only so the Snap-BI webhook middleware can
be tested against a real SHA256withRSA signature. They are not credentials: they
guard nothing and are safe to publish. Never reuse them.
